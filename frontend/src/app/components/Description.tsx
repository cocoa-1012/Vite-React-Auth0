function Description({ text }: { text: string }) {
  return (
    <div className="text-white3 text-[16px] font-normal font-poppins mb-10 text-center">
      {text}
    </div>
  );
}

export default Description;
